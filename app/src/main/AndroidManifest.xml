<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.firstfm">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".FMApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FirstFM"
        android:usesCleartextTraffic="true">
        <activity android:name=".artistdetails.view.ArtistDetailsActivity"/>
        <activity android:name=".albumdetails.view.AlbumDetailsActivity" />
        <activity android:name=".genredetails.view.GenreDetails"/>
        <activity android:name=".main.view.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>